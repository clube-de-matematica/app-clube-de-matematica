/// Objeto que contém as constantes utilizadas nas preferências do usuário.
abstract class PrefConst {
  /// Nome do campo que armazena o nome do usuário.
  static const kUserName = 'user_name';
  /// Nome do campo que armazena o email do usuário.
  static const kUserEmail = 'user_email';
}
