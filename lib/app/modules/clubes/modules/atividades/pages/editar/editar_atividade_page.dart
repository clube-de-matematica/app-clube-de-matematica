import 'package:flutter/material.dart';

import '../../models/atividade.dart';

class EditarAtividadePage extends StatelessWidget {
  const EditarAtividadePage(this.atividade, {Key? key}) : super(key: key);

  final Atividade atividade;

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('data'),
    );
  }
}
